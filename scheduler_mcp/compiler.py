"""Compiler / registration (BUILD_BRIEF.md commit 8).

Depuis une entree en langage naturel : classer le type (notification|script|agent),
compiler le payload, scoper le toolset (least privilege), ecrire la raison de classif.
Script sensible cree directement en statut a_valider. A implementer.
"""
